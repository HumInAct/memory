#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../speller/bin/speller" "$@"
  ret=$?
else 
  node  "$basedir/../speller/bin/speller" "$@"
  ret=$?
fi
exit $ret
